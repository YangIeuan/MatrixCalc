#include "mmm_opt.h"

MATRIX MmultMOpt(MATRIX A, MATRIX B, MATRIX C)
{
    int row_A = A.size();
    int row_B = B.size();
    int col_B = B[0].size();

    for (int i = 0; i < row_A; ++i) {
        
    }
}